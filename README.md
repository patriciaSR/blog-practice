# Blog Practice

## Install Depencencies
`npm install -g node`
`npm install express`
`npm install mongodb`
`npm install cors`

## Install DevDepencencies
`npm install --save-dev jest`
`npm install --save-dev eslint`


